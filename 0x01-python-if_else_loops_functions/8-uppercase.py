#!/usr/bin/python3
def uppercase(str):
    """checks for upper case characters"""
    for i in str:
        if i
    if c >= chr(65) and c <= chr(90):
        return True
    else:
        return False
